<svg>
    <image x="@Model.X" y="@Model.Y" height="@(Model.Height)px" width="@(Model.Width)px" href="GameAssets/Images/@(Model.SpriteName).svg"></image>
    <rect x="@Model.CollisionRectangle.X" y="@Model.CollisionRectangle.Y" height="@(Model.CollisionRectangle.Height)px" width="@(Model.CollisionRectangle.Width)px" style="stroke:#006600; fill:none"></rect>
</svg>

@code {
    [Parameter]
    public GameModelBase Model { get; set; }

}
